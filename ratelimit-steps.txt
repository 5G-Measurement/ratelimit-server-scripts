Steps:

1- Go to Azure portal: Make virtual machine

2- In networking, add firewall rule for 5201 and modify for 443

3- SSH to the machine

4- Setup the server
    sudo yum install -y git && git clone https://github.com/5G-Measurement/ratelimit-server-scripts.git && cd ratelimit-server-scripts && sudo bash ratelimit-server-setup.sh

5- Do a complete set of tests using run commands

6- Copy logs from server
    scp -r arvind@[IP]:/home/arvind/ratelimit-server-scripts/iperf3logs/ .

7- Go to Azure portal again: delete the virtual machine