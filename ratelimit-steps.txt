Steps:

1- Go to Azure portal: Make virtual machine

2- In networking, add firewall rule for 5201 and modify for 443

3- SSH to the machine

4- Install git
    sudo yum install -y git

5- Setup for iperf3.9
    git clone https://github.com/5G-Measurement/ratelimit-server-scripts.git && cd ratelimit-server-scripts && sudo bash ratelimit-server-setup.sh

6- Setup for ndt7-server
    sudo bash setup-ndt-server.sh

7- Setup for faketcp
    cd ..
    git clone https://github.com/ahmadhassan997/rawtcp-udp.git


8- Do a complete set of tests using run commands

9- Copy logs from server
    scp -r arvind@[IP]:/home/arvind/ratelimit-server-scripts/iperf3logs/ .

10- Go to Azure portal again: delete the virtual machine